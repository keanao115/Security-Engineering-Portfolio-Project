# CyberMind AI 企業級成熟度稽核清單(供 AI 助手執行檢查與修正)

## 使用說明(給執行這份文件的 AI 助手)

這份文件是一份**可執行的稽核清單**,不是單純的評論報告。請依照以下方式使用:

1. 針對下方每一個「維度」的每一項檢查點,實際進到這個專案的原始碼(前端 `src/`、後端 `server/src/`、`.github/`、根目錄設定檔)去查驗,不要只憑檔名或 README 猜測
2. 每一項檢查點請標記狀態:`✅ 已達標` / `⚠️ 部分達標` / `❌ 未達標` / `➖ 不適用`
3. 對於 `⚠️` 或 `❌` 的項目,請直接提出修正方案,若使用者同意,直接修改程式碼並補上對應測試
4. 完成一輪後,請輸出一份「現況對照表」(每個維度 + 狀態 + 一句話說明),讓使用者可以快速看到全貌
5. 這是一個學生 / 求職作品集專案,不是真正要上線服務大量使用者的商業系統,所以「企業級」的判準應該是**工程紀律與正確的做法有沒有被實踐**,而不是要求它具備大公司才需要的規模化基礎設施(例如不需要多區域容災、不需要服務網格)。請用這個尺度去評估,避免建議過度工程化的東西。

---

## 維度一:身分驗證、授權與存取控制(AuthN / AuthZ)

**為什麼重要**:這是資安產品最基本的信任基礎,如果連自己的存取控制都做不好,其他功能再多也沒有說服力。

檢查點:
- [ ] 所有需要登入的 API 路由,是否都確實掛了驗證中介層(逐一檢查 `server/src/index.ts` 裡每個 `app.use('/api/...')` 的掛載,確認沒有遺漏,也沒有「相容性路由」之類重複掛載但漏掉驗證的情況)
- [ ] 找不到合法憑證時,是否明確回傳 401,而不是給予任何形式的預設身分
- [ ] 是否有角色分級(至少 2–3 種角色),且角色檢查是否真的套用在「會修改資料、觸發外部服務呼叫、或改變系統設定」的端點上,而不是只定義了函式卻沒人用
- [ ] token 是否有合理的過期時間,並確認前端有處理 token 過期後的重新登入流程
- [ ] 是否有基本的密碼強度規則(如果有帳號密碼登入的話),密碼是否用 bcrypt/argon2 等雜湊儲存而非明碼

---

## 維度二:密鑰與敏感資料管理

**為什麼重要**:密鑰外洩是最常見也最容易被面試官抽問的資安基本功。

檢查點:
- [ ] 原始碼裡搜尋是否還有任何硬編碼的密鑰/密碼/API key(`grep -rn "SECRET\|API_KEY\|PASSWORD" --include="*.ts" --include="*.js" server/src src`,排除掉讀取 `process.env` 的合法用法)
- [ ] 缺少必要環境變數時,伺服器是否會直接拒絕啟動並給出清楚錯誤訊息,而不是用預設值悄悄運作
- [ ] `.gitignore` 是否正確排除所有 `.env` 類檔案、`node_modules`、建置產物(`dist/`、`build/`)
- [ ] 版本歷史(`git log -p`)裡是否曾經 commit 過真正的密鑰(即使現在刪除了,舊 commit 裡仍然存在,需要的話要用 `git filter-repo` 之類工具清除歷史)
- [ ] 前端(瀏覽器可讀取的程式碼)裡是否不小心把後端才該持有的密鑰打包進去了

---

## 維度三:輸入驗證、輸出處理與常見漏洞防護

**為什麼重要**:這是 OWASP Top 10 涵蓋的基本功,對一個「安全工程」定位的專案來說,這部分如果做不好會很矛盾。

檢查點:
- [ ] 所有接受使用者輸入的 API(尤其是會拿去組 SQL、組指令、或當作外部請求目標的欄位)是否都有做格式/白名單驗證(例如 `target` 主機參數,是否限制只能掃描白名單內的 IP,防止被當跳板)
- [ ] 是否有使用參數化查詢/ORM,避免 SQL/NoSQL injection(如果有資料庫的話)
- [ ] 是否對使用者可控的檔名、路徑做過濾,避免 path traversal(尤其報告匯出、檔案上傳相關功能)
- [ ] 前端渲染使用者或外部資料時,是否避免直接用 `dangerouslySetInnerHTML` 或等效方式,避免 XSS
- [ ] 是否有基本的速率限制(rate limiting),特別是會呼叫外部付費 API(Gemini、NVD)或執行掃描動作的端點
- [ ] CORS 設定是否明確限制允許的來源,而不是開放所有網域

---

## 維度四:相依套件與供應鏈安全

**為什麼重要**:企業級專案會定期檢查相依套件是否有已知漏洞,這也是最容易自動化、最容易展示的一項。

檢查點:
- [ ] 執行 `npm audit`(前端與後端各跑一次),記錄目前有多少高風險/嚴重漏洞
- [ ] 是否有啟用 Dependabot(GitHub 內建,免費)或類似工具自動偵測套件漏洞並發 PR
- [ ] `package.json` 裡的版本號是否合理鎖定(避免用過於寬鬆的版本範圍導致建置不可重現)
- [ ] 是否有 lockfile(`package-lock.json`)且有被 commit 進版本控制

---

## 維度五:程式碼品質、架構與型別安全

**為什麼重要**:這是判斷「這個人是不是能寫出團隊可維護程式碼」的主要依據。

檢查點:
- [ ] 是否有 ESLint / Prettier 設定檔,並且在 CI 裡有跑 lint 檢查
- [ ] TypeScript 的 `tsconfig.json` 是否開啟 `strict` 模式(這是判斷型別安全認真程度的關鍵指標)
- [ ] 是否存在明顯的死碼(定義了但沒有任何地方引用的檔案/函式),需要清除
- [ ] 前後端的分層是否清楚(routes / services / middleware / utils 各司其職),還是邏輯全部塞在單一大檔案裡
- [ ] 是否有重複實作同一件事的程式碼(例如同一種資料解析邏輯,前後端各寫一份)
- [ ] 命名、資料夾結構是否一致,新加入的工程師能不能在合理時間內看懂架構

---

## 維度六:測試策略與覆蓋率

**為什麼重要**:沒有測試的專案,每次修改都是賭博;企業級專案要求改動有安全網。

檢查點:
- [ ] 是否有針對核心邏輯(認證、權限、資料解析、評分公式)的單元測試,而不是只測試最簡單的路徑
- [ ] 是否有針對「已經修好的漏洞」補上對應的回歸測試(防止未來重新引入同樣的問題)
- [ ] 是否有整合測試,至少涵蓋幾條關鍵的 API 呼叫流程(登入 → 呼叫受保護端點 → 驗證回應)
- [ ] 測試覆蓋率大致落在什麼範圍(不需要 100%,但核心模組建議至少有基本覆蓋);可以用 `c8` 或 `nyc` 產生覆蓋率報告
- [ ] CI 裡是否強制測試通過才能合併(而不是只是跑過,失敗也不擋)

---

## 維度七:CI/CD 與部署流程

**為什麼重要**:能否自動化建置、測試、部署,是團隊協作與生產穩定度的關鍵指標。

檢查點:
- [ ] `.github/workflows/` 裡的 CI 是否涵蓋:型別檢查、lint、測試、建置,四項缺一不可
- [ ] 是否有區分不同環境(開發/預備/正式)的設定,而不是全部共用同一份 `.env`
- [ ] `deploy/` 底下的 Docker / Kubernetes 設定,是否與實際程式碼保持同步(檢查有沒有寫死已經不存在的服務名稱或 port)
- [ ] Docker image 是否用了多階段建置(multi-stage build)來縮小最終 image 大小,並且不是用 `root` 使用者執行容器
- [ ] 是否有基本的部署文件,說明如何從零把系統跑起來(README 裡的 Quick Start 是否經過實際驗證可行)

---

## 維度八:可觀測性(日誌 / 監控 / 錯誤追蹤)

**為什麼重要**:企業級系統出問題時,能不能快速定位原因,取決於這一層做得好不好。

檢查點:
- [ ] 後端是否有結構化日誌(例如用 `pino`/`winston`),而不是散落各處的 `console.log`
- [ ] 錯誤是否有集中處理的機制(Express 的錯誤處理中介層),避免未捕捉的例外讓伺服器直接崩潰或洩漏堆疊資訊給使用者
- [ ] 日誌裡是否小心避免把敏感資訊(密碼、token、個資)寫進去
- [ ] `deploy/` 裡提到的 Prometheus/Grafana 設定,是否真的有對應的 metrics 端點在後端程式碼裡實作(`/metrics`),還是只有部署設定檔存在但後端沒有真的暴露指標
- [ ] 是否有基本的健康檢查端點(`/health` 或 `/status`),並且回傳內容不包含敏感資訊

---

## 維度九:錯誤處理與系統韌性

**為什麼重要**:外部服務(Gemini API、NVD API)不可能永遠可用,系統要有優雅降級的能力。

檢查點:
- [ ] 呼叫外部 API(Gemini、NVD)時是否有 timeout 設定,避免請求卡住不放
- [ ] 外部 API 失敗時,是否有清楚的錯誤訊息回傳給前端,而不是整個功能直接掛掉
- [ ] 是否有基本的重試機制(對暫時性錯誤),但避免無限重試造成雪崩
- [ ] 前端對 API 失敗的處理是否友善(loading/error 狀態明確),而不是白畫面或無限轉圈

---

## 維度十:效能、可擴展性與資料管理

**為什麼重要**:即使是作品集專案,展示對效能與資料設計的基本意識也是加分項。

檢查點:
- [ ] 資料庫查詢(如果有)是否有針對常用查詢欄位建立索引
- [ ] 是否有分頁機制處理大量資料的 API(例如 IOC 資料庫、日誌查詢),而不是一次回傳全部
- [ ] 大量資料匯入/掃描這類耗時操作,是否有做成非同步/背景工作,而不是讓 HTTP 請求整個卡住等結果
- [ ] 前端是否有針對大量資料的列表做虛擬捲動或分頁,避免一次渲染大量 DOM

---

## 維度十一:API 設計與文件

**為什麼重要**:好的 API 設計與文件,是團隊協作與其他系統整合的基礎。

檢查點:
- [ ] API 路徑、命名是否遵循一致的慣例(RESTful 或至少內部一致)
- [ ] 是否有 API 文件(OpenAPI/Swagger 規格,或至少一份清楚列出所有端點、參數、回應格式的 Markdown)
- [ ] 錯誤回應格式是否統一(例如都用 `{ error: { code, message } }` 這種固定結構),而不是每個端點各寫各的
- [ ] API 版本管理策略是否有想過(即使目前用不到,能不能講出你的規劃)

---

## 維度十二:版本控制、協作與治理流程

**為什麼重要**:這一項展示的是「你懂不懂團隊怎麼一起開發」,不只是自己寫得出來。

檢查點:
- [ ] commit 歷史是否有意義(訊息清楚描述改了什麼),而不是一堆 `fix`、`update` 這種無資訊量的訊息
- [ ] 是否有使用分支策略(即使是單人開發,至少展示 feature branch → PR → merge 的流程)
- [ ] 是否有 `CONTRIBUTING.md` 或至少在 README 說明如何貢獻/開發流程
- [ ] 是否有 CHANGELOG 或 Release 紀錄,讓人看得出版本演進
- [ ] License 檔案是否存在且選擇合理

---

## 維度十三:專案文件與 Onboarding

**為什麼重要**:文件品質往往是面試官/招募方第一眼會看的東西,比程式碼本身更早被評估。

檢查點:
- [ ] README 的用詞是否誠實對應實際完成度(避免「enterprise-grade」這類與實際規模不符的形容詞,參考專案自己提出的「Truthful Implementation Status Matrix」精神,延伸到整份 README)
- [ ] 是否有架構圖(即使是簡單的文字方塊圖),讓人 10 秒內看懂系統組成
- [ ] Quick Start 步驟是否完整到「一個沒看過這個專案的人,照著做真的能跑起來」
- [ ] 是否清楚說明 DEMO 模式與 LIVE 模式的差異,以及各自需要什麼設定
- [ ] 是否有一段「已知限制 / 未來規劃」,誠實說明目前做不到什麼

---

## 給 AI 助手的最終輸出格式建議

完成上述十三個維度的檢查後,請用以下格式輸出總結給使用者:

```
| 維度 | 狀態 | 一句話說明 | 建議優先處理項目 |
|---|---|---|---|
| 一、AuthN/AuthZ | ✅/⚠️/❌ | ... | ... |
| 二、密鑰管理 | ... | ... | ... |
...
```

並在表格後面,用「若只能挑三件事優先做,建議做哪三件」的方式,給出一個聚焦的行動建議,而不是把十三個維度的所有項目都丟給使用者——作品集專案的時間有限,重點是先把「信任基礎」(維度一、二、三)做扎實,再逐步往其他維度補強。
